# Release Notes v0.0.103

- The CLI is now published as **`notefig`** — pair a machine with `npx notefig agent` (the app's pairing dialog reflects this).
- Finished the internal rename to Notefig across the app and agent tooling. Agent-authored interactive blocks now use `notefig:` fences; blocks created before this update render as plain code — re-author them if needed.
