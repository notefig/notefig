# What's New in v0.0.95

- Workspaces open faster: file metadata is hydrated lazily instead of up front.
- Non-content files (dependency and build directories, unsupported file types) are now ignored, keeping the file tree and search focused on your writing.
